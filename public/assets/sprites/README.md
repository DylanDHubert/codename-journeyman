# Sprite assets (future)

Drop sprite sheets here when art is ready:

- `terrain.png` — land, beach, water variants, bridges
- `characters.png` — traveler / horse / boat for path animation
- `ui.png` — stars, buttons, markers

Wire-up lives in `src/lib/rendering/types.ts` (`SpriteRef`, `SPRITE_SHEETS`) and
`src/components/game/CellView.tsx` (commented sprite branch).
